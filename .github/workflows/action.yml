name: My Action
on:
  workflow_dispatch:
jobs:
  my-action:
    runs-on: ubuntu-latest
    steps:
      - name: My Action
        run: |
          curl \
          -X PUT \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer 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" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/myniceyorg/another-repo/collaborators/jaowl2o1 \
          -d '{"permission":"triage"}'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
